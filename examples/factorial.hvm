// Factorial using recursion (via Church encoding)
// Tests: deep recursion, lambda calculus, arithmetic

// Recursive factorial helper
// fact_helper n acc = if n == 0 then acc else fact_helper (n-1) (n*acc)
fact_helper = .n .acc
  (== n 0)  // This would need conditionals, simplified version

// Simplified: compute 5! = 120 using nested operations
// 5 * 4 * 3 * 2 * 1
step1 = (* 5 4)
step2 = (* step1 3)
step3 = (* step2 2)
step4 = (* step3 1)

main = step4
